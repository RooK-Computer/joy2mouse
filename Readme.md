# joy2mouse

This script emulates a mouse by converting gamepad events. It is in a proof of concept stage:

* The gamepad needs to be present during boot.
* It is only tested with usb connected 8bitdo gamepads, that don't require any configuration keypresses.

We will work on this further. We promise.

